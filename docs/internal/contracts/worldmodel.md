# WorldModel スキーマ契約(分割済み)

2026-07-07 にドメイン別のファイルへ分割した。節ごとの移動先は
[README.md](README.md) の対応表を参照。本ファイルには今後定義を書かない。

分割前の変更履歴を追う場合は `git log` の旧パス(このファイル)を参照する。
凍結された歴史的記録(`../plans/`)からの参照を解決するため、本ファイルは
恒久的に残す(内容はこのスタブのみ)。
